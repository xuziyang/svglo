# 2026-07-24

## SEO 文案去"浏览器"化 + 续写文章
- 用户要求：不要在文案中强调"在浏览器中运行"，并继续此前的工作。
- 判断此前工作为 svglo 的 SEO 长文（`src/i18n/article.ts`，由 `docs/svg_converter_roi_20260724.csv` 关键词 ROI 驱动）。
- 改动（EN + ZH 同步）：
  - `src/i18n/article.ts`：去掉 "right in your browser" / "在浏览器中"，改为 "on your device" / "在本地"；保留本地/私密/不上传的卖点。新增 `tips` 小节（"Tips for cleaner vector traces" / "让矢量图更干净的小技巧"），以及 SVG→PNG/JPG 的 FAQ（诚实说明本工具只做 image→SVG，回退方法用矢量软件导出）。
  - `src/i18n/en.ts`、`src/i18n/zh.ts`：hero/dropzone 文案去浏览器化（"图片不离开你的设备" 等）。
- 已 `npm run typecheck` 通过、`npm run build` 成功；rebuilt dist 的 en/zh HTML 已不含浏览器强调措辞。

## 第二轮：进一步去"本地/隐私/不上传"化
- 用户补充："也不要强调本地等" / "也不要体现本地等" → 连"本地、不上传、隐私、设备、服务器"这类卖点也全部去掉，只保留转换能力与质量/参数/用途本身。
- 改动（EN + ZH）：
  - `src/i18n/article.ts`：intro 去掉"本地完成/无需上传/保护隐私"；features 段去掉"server-based/WebAssembly 在本地/离开设备/机密客户"并改标题 "Fast, private, and precise"→"Fast and precise"、"快速、私密、精准"→"快速、精准"；删除"图片是否上传到服务器"这条 FAQ（纯隐私向）；保留 no watermark/no signup 等中立表述。
  - `src/i18n/en.ts`、`src/i18n/zh.ts`：hero lead / featLocal 徽章 / dropzone hint 去本地化，改为能力向表述（"精准矢量描边，数秒出图" 等）。
- 验证：build 通过；dist 中仅余"上传图片"按钮标签、viewport 的 device-width、代码内 server 字符串，均非强调性文案。README/CLAUDE.md 仍为事实性架构描述，未改（如需要可再处理）。
- 注意：svglo 只做 image→SVG，不要为 SVG→PNG/JPG 编造功能，FAQ 用"用矢量软件导出"诚实回答。
